<p id="notice"><%= notice %></p>
<div class="container">

<div class="well">

<h3><%= @pin.name %></h3>
<div class="thumbnail">
<p><%= image_tag(@pin.photo, :style => "width:500px", :class => "img-responsive") %></p>
</div>
<p><%= @pin.description %></p>

<% if user_signed_in? %>
<%= link_to 'Edit', edit_pin_path(@pin) %> |
<%	end %>
<%= link_to 'Back', pins_path %>

<%= link_to "Más Pins del usuario", pinsof_path(@pin.user.id)  %>

</div>
</div>
