
<%= form_for(pin) do |f| %>
  <% if pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pin.errors.count, "errores") %> no permiten guardar tu Pin</h2>

      <ul>
      <% pin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
  <div class="field">
    <%= f.label :name, "Nombre" %><br>
    <%= f.text_field :name, :class => 'form-control', :placeholder => "Nombre del Pin" %>
  </div>
</div>

<div class="form-group">
  <div class="field">
    <%= f.label :photo, "Foto" %>
    <%= f.text_field :photo, :class => "form-control", :placeholder => "Url de la foto" %>
  </div>
</div>

<div class="form-group">
  <div class="field">
    <%= f.label :description, "Descripción" %>
    <%= f.text_area :description, :class => "form-control", :placeholder => "Descripción del Pin", :rows => "5", :cols => "30" %>
  </div>
</div> 

  

  <div class="actions">
    <%= f.submit "Crear", :class => "btn btn-primary" %>
  </div>
<% end %>
